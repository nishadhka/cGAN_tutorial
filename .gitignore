# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# Jupyter
.ipynb_checkpoints/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
*.tfvars
!*.tfvars.example

# Environment files
.env
*.env

# Service account keys / credentials — NEVER commit
env/key-*.json
env/*-key.json
env/*-credentials.json
env/application_default_credentials.json
coiled-data.json
**/coiled-data.json
example_notebooks/pytorch_cgan/.env
example_notebooks/pytorch_cgan/*.log
example_notebooks/pytorch_cgan/ecmwf_pytorch_cgan_*.json

# Large data files
*.nc
*.h5
*.pkl
*.parquet
*.zarr/
*.tfrecords

# Local test artifacts / scratch
local_tfrecords_test/
2026-*-tfrecords-*.txt

# Local python environments (the tf215gpu micromamba env lives outside the repo;
# this guards any venv created inside the repo). NOTE: the tracked top-level
# `env/` dir is project build config, NOT a python env, and is intentionally kept.
.venv/
.venv-*/
venv/
