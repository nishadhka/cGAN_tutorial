# ECMWF GIK Tutorial - Environment Configuration
# Copy this file to .env and fill in your values

# GCS Bucket Configuration for storing parquet outputs
# The parquet files will be uploaded to: gs://{GCS_BUCKET}/{GCS_PARQUET_PREFIX}/{YYYYMMDD_runz}/
GCS_BUCKET=gik-ecmwf-aws-tf
GCS_PARQUET_PREFIX=run_par_ecmwf

# Path to GCS service account JSON file (required for GCS upload and Coiled workers)
GCS_SERVICE_ACCOUNT_FILE=coiled-data.json

# Default GCS parquet path for Coiled streaming
GCS_PARQUET_PATH=gs://gik-ecmwf-aws-tf/run_par_ecmwf/20260203_00z

# Coiled Configuration (for parallel processing)
COILED_WORKSPACE=gcp-sewaa-nka
COILED_REGION=us-east1
